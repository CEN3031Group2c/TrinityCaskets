import React from 'react';
import Listings from "../../components/Listings/Listings"

export default ({input}) => {
    return <Listings type="" input = {input}/>
    return <div className="App" className="centered">
                <Listings type="" input = {input}/>
            </div>
}